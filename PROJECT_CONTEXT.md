# Vibe Ledger 项目上下文与 Agent 交接

> 更新日期：2026-08-16  
> 当前阶段：需求澄清中，尚未确定最终架构，也未开始重构。  
> 后续 Agent 应先阅读本文，再阅读代码；`development_doc.md` 是较早方案，不代表最新决策。

## 1. 项目目标

面向两人的家庭 AI 账本。日常通过 iPhone Shortcut 上传单笔消费截图，服务端用多模态模型识别并记账；每月在 Dashboard 上传各账户的银行/信用卡 Statement PDF，匹配已有交易、补录遗漏并完成对账。系统关注家庭整体资产、负债、收支和投资估值，不做夫妻分账、AA 或待结算管理。

## 2. 当前代码与架构

```text
iPhone Shortcut
  -> POST Base64 截图 + 备注 + 可选幂等键
  -> FastAPI (`ai-ledger-backend/main.py`)
  -> Gemini 结构化识别
  -> PostgreSQL/Supabase (`accounts` + `transactions`)

Streamlit (`ai-ledger-dashboard/app.py`)
  -> 直接读写同一数据库
  -> 资产负债、收支、信用卡统计、手动余额校准
```

- 后端：FastAPI、Gemini、psycopg2；当前部署目标为 Hugging Face Spaces。
- 前端：Streamlit；直接访问数据库。
- 数据模型：仅 `accounts`、`transactions` 两张核心表，账户余额由写流水时直接增减。
- 账户、账户类型、分类及识别别名硬编码在 Python 类型和 Gemini Prompt 中。
- 现有交易类型：`expense`、`income`、`transfer`、`adjustment`。
- 当前技术栈只是免费且可用的起点，不是约束；最终方案可替换部分或全部组件。

## 3. 已确认的产品决策

### 3.1 用户、账户与报表

- 两人各有个人账户，但不记录每笔交易的付款人或实际消费者。
- 不区分家庭/个人支出，不处理垫付、AA、夫妻债权债务。
- 账户可带“所有者”元数据，仅用于筛选、默认账户集合和展示。
- 需要家庭总报表，也需要任意选择一个或多个账户进行汇总透视。
- 两个轻量登录账户；浏览器保持登录；两部 iPhone 使用独立设备令牌。
- 两人都可查看全部家庭数据，并可拥有不同的默认币种和账户筛选。

### 3.2 数据入口与确认流程

- Shortcut 只负责上传单笔消费截图。
- 高置信度结果直接正式入账。
- 低置信度结果进入待确认；批准前不改余额、不进入报表。
- Dashboard 提供待办列表，可批准、修改后批准或拒绝。
- Dashboard 每月提供各账户 Statement PDF 上传入口。
- Statement 处理目标：匹配已有交易 -> 补录遗漏 -> 处理未识别条目 -> 校准余额/账单。
- 同一消费先由 Shortcut 记录、后出现在 Statement 时，只做匹配确认，不能重复入账。

### 3.3 币种与汇率

- 家庭汇总的主要口径为 CNY。
- 每个账户及每笔交易保留原始币种。
- 一个账户只对应一种币种；多币种银行账户人工拆为多个账户，如 `DBS_SGD`、`DBS_USD`。
- 资产总额按当前或 T-1 公共参考汇率折算，只用于了解当前家庭资产价值。
- 不重点核算或展示汇兑损益。
- 外币信用卡消费保留原币金额；还款记录实际转出币种金额和信用卡债务减少金额；还款不重复计入消费。
- 当前月消费可按当前/T-1 参考汇率折算；历史月份是否冻结汇率尚未决定。

### 3.4 消费、收入、转账与信用卡

- 工资、奖金、利息、礼金、公司报销均作为收入。
- 自有账户间转账和信用卡还款不计入收入或消费。
- 信用卡日常按月全额还清。
- 不管理最低还款、自动还款状态、逾期、年费、利息、手续费或复杂多币种账单状态。
- 日常消费用于推算信用卡账单；每月 Statement 的已出账单金额是权威数据并用于校准。

### 3.5 分期、删除与退款

- 分期消费按月确认支出，例如 12,000/12 期每月计 1,000 消费。
- 分期手续费如出现，单独记账。
- 第一阶段不支持提前还清。
- 不单独建立退款流程；用户通过删除原消费处理退款。
- 删除允许改写历史报表，但内部必须逻辑删除并保留审计痕迹。
- 删除分期购买时，撤销已入账期次并取消所有未来期次。
- 第一期归属月份及信用卡负债显示全部剩余本金还是仅已出账部分，尚未决定。

### 3.6 Statement 对账

- 先利用 Statement 明细匹配并补录漏记交易，再计算剩余差异。
- 现金、储蓄、信用卡等普通账户：最终差异折合不超过 200 CNY 时自动记为“对账差异”；超过则人工确认。
- 对账差异修正余额，但默认不进入消费分类统计；之后可人工改为具体消费。
- 投资账户不受 200 CNY 阈值限制。
- 多账户对账时，每个账户独立判断阈值。

### 3.7 投资

- 第一阶段只记录投资/股票账户的定期总资产快照和净值曲线。
- 市值变化进入投资盈亏及净资产视图，不作为工资等现金收入。
- 架构需保留未来扩展持仓、数量、成本、行情和自动市值计算的能力。

### 3.8 配置、隐私与数据管理

- Dashboard 支持创建、编辑、停用账户；停用账户保留历史数据。
- 账户可配置类型、币种、所有者、信用卡账单日及识别别名。
- 收入和消费分类可自定义。
- Shortcut 高置信度截图处理后立即删除；低置信度截图只保留到人工处理完成。
- Statement PDF 的具体留存和密码处理规则仍待确认。
- 接受数据库结构调整和数据迁移。

## 4. 当前实现的关键缺口

1. 没有用户、设备、账户所有者、登录或 API 鉴权。
2. 没有待确认状态、置信度规则、交易编辑/逻辑删除和完整审计链。
3. 没有 Statement PDF 导入、批次、明细、匹配或对账工作流。
4. SGD 未实现；Dashboard 仅部分支持 USD/CNY，收支未正确进行多币种折算。
5. 数据库有跨币种双边金额字段，但 Gemini 输出模型没有对应字段，当前依赖备注正则补全。
6. 多账户截图对账存在幂等缺陷：第一个账户提交后，后续账户可能因父幂等键重复而失败。
7. 当前分期会生成未来流水并立即影响全部账户余额，混合了“月度确认支出”和“当前负债”口径，且存在分摊舍入误差。
8. 信用卡账单仅由交易日期区间推算，无法表达权威 Statement、匹配状态和校准结果。
9. 账户和分类硬编码，后端与 Dashboard 重复数据库逻辑，容易漂移。
10. Dashboard 正式代码在 `app.py`，Docker 启动入口却是仍为示例页的 `src/streamlit_app.py`。
11. API 会同步调用 Gemini/数据库，错误处理、输入限制、文件安全和后台任务机制不足。
12. 现有集成测试会直接写入/删除远端 `_dev` 数据；未获确认前不要运行。仓库内虚拟环境仍指向旧电脑路径，不可复用。

## 5. 尚待确认的问题

1. 首批支持哪些银行、信用卡和券商；Statement PDF 是否含完整明细、是否扫描件、是否有密码。
2. 上传 Statement 时是否由用户明确选择账户；一份多币种 PDF 如何映射多个子账户。
3. Statement 是否自动导入消费、收入、内部转账、信用卡还款等全部流水类型。
4. 自动匹配容差：建议同账户、金额/币种优先、日期允许约 ±5 天、商户模糊匹配；多候选进入待确认。
5. 银行 Statement 的期末余额与信用卡 Statement 的本期应还/当前欠款应如何分别作为权威值。
6. 分期第一期从购买月还是首次出账月开始；当前负债显示全部剩余分期还是仅已出账部分。
7. 历史收支使用动态当前汇率，还是月末冻结汇率。
8. 投资快照通过 Dashboard 上传截图、手工填写，还是两者都支持。
9. Statement PDF 是否解析后立即删除；失败/待确认时最长保留多久；密码如何临时使用。
10. 现有生产数据规模、是否必须全部保留、迁移可接受的停机窗口。
11. 最终技术栈选型：比较现有栈与替代方案的免费额度、隐私、后台任务能力、维护成本和迁移难度。

## 6. 下一步

1. 继续确认第 5 节问题，必要时提供脱敏 Statement 样本。
2. 形成一份最终“需求确认稿”，由用户确认无误。
3. 再设计目标业务流程、数据模型、技术架构、AI/PDF 解析与匹配机制。
4. 给出旧数据迁移方案和分阶段实施路线。
5. 用户确认方案后再开始编码；当前阶段不要直接重构。

